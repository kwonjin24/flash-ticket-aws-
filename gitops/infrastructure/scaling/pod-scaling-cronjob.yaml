# 파드 스케일업 CronJob (오후 4:50)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pod-scale-up
  namespace: flash-ticket
spec:
  schedule: "17 8 * * *"  # UTC 08:17 (KST 17:17, NodePool 생성 2분 후)
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: scaling-operator
          containers:
          - name: kubectl
            image: bitnami/kubectl:latest
            command:
            - /bin/sh
            - -c
            - |
              echo "🚀 파드 스케일업 시작..."
              
              # 현재 HPA 상태 확인
              echo "=== 스케일업 전 HPA 상태 ==="
              kubectl get hpa -n flash-ticket
              
              # HPA minReplicas 증가로 강제 스케일업
              echo "=== HPA 피크 모드 활성화 ==="
              kubectl patch hpa flash-api-hpa -n flash-ticket -p '{"spec":{"minReplicas":6}}'
              kubectl patch hpa flash-gateway-hpa -n flash-ticket -p '{"spec":{"minReplicas":10}}'
              kubectl patch hpa flash-pay-hpa -n flash-ticket -p '{"spec":{"minReplicas":3}}'
              
              # 결과 확인
              echo "=== 스케일업 후 상태 ==="
              kubectl get hpa -n flash-ticket
              kubectl get deployments -n flash-ticket
              
              echo "✅ 파드 스케일업 완료!"
          restartPolicy: OnFailure
---
# 파드 스케일다운 CronJob (오후 5:30)
apiVersion: batch/v1
kind: CronJob
metadata:
  name: pod-scale-down
  namespace: flash-ticket
spec:
  schedule: "47 8 * * *"  # UTC 08:47 (KST 17:47, 30분 후 스케일다운)
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: scaling-operator
          containers:
          - name: kubectl
            image: bitnami/kubectl:latest
            command:
            - /bin/sh
            - -c
            - |
              echo "📉 파드 스케일다운 시작..."
              
              # 현재 HPA 상태 확인
              echo "=== 스케일다운 전 HPA 상태 ==="
              kubectl get hpa -n flash-ticket
              
              # HPA minReplicas 원복으로 자동 스케일다운
              echo "=== HPA 일반 모드 복원 ==="
              kubectl patch hpa flash-api-hpa -n flash-ticket -p '{"spec":{"minReplicas":2}}'
              kubectl patch hpa flash-gateway-hpa -n flash-ticket -p '{"spec":{"minReplicas":4}}'
              kubectl patch hpa flash-pay-hpa -n flash-ticket -p '{"spec":{"minReplicas":2}}'
              
              # 결과 확인
              echo "=== 스케일다운 후 상태 ==="
              kubectl get hpa -n flash-ticket
              kubectl get deployments -n flash-ticket
              
              echo "✅ 파드 스케일다운 완료!"
          restartPolicy: OnFailure