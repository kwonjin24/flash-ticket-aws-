{
  "name": "flash-tickets",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,magenta \"pnpm --dir api start:dev\" \"pnpm --dir web dev --host 127.0.0.1\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "api",
    "web"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "packageManager": "pnpm@10.17.1"
}
