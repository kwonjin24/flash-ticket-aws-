{
  "name": "flash-tickets",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web,pay -c blue,magenta,green \"pnpm --dir api start:dev\" \"pnpm --dir web dev --host 127.0.0.1\" \"pnpm --dir pay dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "api",
    "web",
    "pay"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "packageManager": "pnpm@10.17.1"
}
